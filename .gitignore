.playwright-mcp/

# Metrics App exports contain tenant data (workspace, item and user names).
# Keep them local — never commit them.
*.csv

# Raw walkthrough screenshots are embedded as data URIs in the published HTML
# under docs/capacity-diagnosis/, so committing them again just doubles the size.
docs/capacity-diagnosis/screenshots/
